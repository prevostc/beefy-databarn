{{
  config(
    materialized='view',
  )
}}

SELECT
  assets,
  risks,
  migration_ids,
  point_structure_ids,
  zaps,
  id,
  name,
  type,
  token,
  token_address,
  token_decimals,
  token_provider_id,
  earn_contract_address,
  earned_token,
  earned_token_address,
  oracle,
  oracle_id,
  status,
  created_at,
  platform_id,
  strategy_type_id,
  add_liquidity_url,
  remove_liquidity_url,
  network,
  is_gov_vault,
  chain,
  strategy,
  price_per_full_share,
  last_harvest,
  retire_reason,
  retired_at,
  buy_token_url,
  lending_oracle__provider,
  lending_oracle__address,
  paused_at,
  curator_id,
  underlying_platform_url,
  updated_at,
  deposit_fee,
  pause_reason,
  refund,
  refund_contract_address,
  show_warning,
  warning,
  earning_points,
  pool_together,
  retired_reason,
  bridged__optimism,
  bridged__base,
  bridged__sonic
FROM dlt.beefy_api___vaults

