{{
  config(
    materialized='view',
  )
}}

SELECT
  assets,
  risks,
  migration_ids,
  point_structure_ids,
  deposit_token_addresses,
  zaps,
  id,
  name,
  token,
  token_address,
  token_decimals,
  token_provider_id,
  earned_token,
  earned_token_address,
  earn_contract_address,
  oracle,
  oracle_id,
  status,
  created_at,
  platform_id,
  strategy_type_id,
  network,
  type,
  fee_tier,
  is_gov_vault,
  chain,
  strategy,
  last_harvest,
  retire_reason,
  retired_at,
  earning_points,
  updated_at
FROM dlt.beefy_api___cow_vaults

