{
  "version": "10",
  "credential": {
    "accessKey": "${MINIO_ACCESS_KEY}",
    "secretKey": "${MINIO_SECRET_KEY}"
  },
  "region": "us-east-1",
  "browser": "on",
  "logger": {
    "console": {
      "level": "info"
    },
    "file": {
      "level": "info",
      "filename": "/var/log/minio/minio.log"
    }
  },
  "notify": {}
}
