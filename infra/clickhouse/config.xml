<?xml version="1.0"?>
<clickhouse>
    <logger>
        <level>information</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <console>1</console>
        <size>1000M</size>
        <count>10</count>
    </logger>

    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <listen_host>0.0.0.0</listen_host>

    <max_connections from_env="CLICKHOUSE_MAX_CONNECTIONS" replace="1">200</max_connections>
    <keep_alive_timeout>3</keep_alive_timeout>
    <max_concurrent_queries from_env="CLICKHOUSE_MAX_CONCURRENT_QUERIES" replace="1">50</max_concurrent_queries>
    <uncompressed_cache_size from_env="CLICKHOUSE_UNCOMPRESSED_CACHE_SIZE" replace="1">8589934592</uncompressed_cache_size>
    <mark_cache_size from_env="CLICKHOUSE_MARK_CACHE_SIZE" replace="1">5368709120</mark_cache_size>

    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>

    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
    <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>

    <user_directories>
        <users_xml>
            <path>/etc/clickhouse-server/users.xml</path>
        </users_xml>
    </user_directories>
    <default_profile>default</default_profile>
    <default_database>default</default_database>

    <timezone>UTC</timezone>

    <mlock_executable>true</mlock_executable>

    <remote_servers>
        <!-- Cluster configuration for future multi-node setup -->
    </remote_servers>

    <zookeeper>
        <!-- Zookeeper configuration for replication (future) -->
    </zookeeper>

    <macros>
        <!-- Cluster macros (future) -->
    </macros>

    <builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>

    <max_session_timeout>3600</max_session_timeout>
    <default_session_timeout>60</default_session_timeout>

    <query_log>
        <database>system</database>
        <table>query_log</table>
        <partition_by>toYYYYMM(event_date)</partition_by>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    </query_log>

    <query_thread_log>
        <database>system</database>
        <table>query_thread_log</table>
        <partition_by>toYYYYMM(event_date)</partition_by>
    </query_thread_log>

    <part_log>
        <database>system</database>
        <table>part_log</table>
    </part_log>

    <distributed_ddl>
        <path>/clickhouse/task_queue/ddl</path>
    </distributed_ddl>

    <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>

    <!-- Enable PostgreSQL table engine for federation -->
    <postgresql>
        <port>5432</port>
    </postgresql>

    <!-- Prometheus metrics endpoint -->
    <prometheus>
        <port>9363</port>
        <endpoint>/metrics</endpoint>
        <metrics>true</metrics>
        <asynchronous_metrics>true</asynchronous_metrics>
        <events>true</events>
        <errors>true</errors>
        <histograms>true</histograms>
        <dimensional_metrics>true</dimensional_metrics>
    </prometheus>
</clickhouse>

