api:
  dashboard: false
  insecure: false
  ping: true

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
    observability:
      accessLogs: false
      metrics: false
      tracing: false
  websecure:
    address: ":443"
    http:
      tls:
        certResolver: letsencrypt
    observability:
      accessLogs: false
      metrics: false
      tracing: false

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    watch: true
    network: beefy-databarn_frontend_network

certificatesResolvers:
  letsencrypt:
    acme:
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web

log:
  level: DEBUG

accessLog: {}

metrics:
  prometheus:
    entryPoint: web
