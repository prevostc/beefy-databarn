config-version: 2
version: 2
sources:
  - name: tap_github_files     # the name we want to reference this source by
    schema: tap_github_files   # the schema the raw data was loaded into
    tables:
      - name: beefy_zap_v3
