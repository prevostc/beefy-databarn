config-version: 2
version: 2
sources:
  - name: tap_beefy_api     # the name we want to reference this source by
    schema: tap_beefy_api   # the schema the raw data was loaded into
    tables:
      - name: beefy_boosts
      - name: beefy_vaults
      - name: beefy_tokens
